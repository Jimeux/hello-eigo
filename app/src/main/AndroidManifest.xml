<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.moobasoft.damego" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.activities.IndexActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.ShowActivity"
            android:label="@string/title_activity_show"
            android:parentActivityName=".ui.activities.IndexActivity"
            android:theme="@style/AppTheme.TransStatusBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activities.IndexActivity" />
        </activity>
        <activity
            android:name=".ui.activities.CommentsActivity"
            android:label="@string/title_activity_comments"
            android:parentActivityName=".ui.activities.ShowActivity"
            android:theme="@style/AppTheme.NoActionBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.moobasoft.damego.ui.activities.ShowActivity" />
        </activity>
        <activity
            android:name=".ui.activities.CreateCommentActivity"
            android:label="@string/title_activity_create_comment"
            android:parentActivityName=".ui.activities.ShowActivity"
            android:theme="@style/AppTheme.NoActionBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.moobasoft.damego.ui.activities.ShowActivity" />
        </activity>
    </application>

</manifest>
